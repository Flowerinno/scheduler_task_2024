export enum ROUTES {
	projects = "/projects",
	project = "/projects/",
	login = "/login",
	logout = "/logout",
	register = "/register",
	profile = "/profile",
	settings = "/settings",
	notFound = "/404",
}
